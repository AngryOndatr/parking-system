# Production profile - active when SPRING_PROFILES_ACTIVE=production
spring:
  datasource:
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    show-sql: false

logging:
  level:
    com.parking.gate_control_service: INFO
    org.springframework.web: WARN
